spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tenant_platform
    username: root
    password: 391281*MySQL@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:change-this-to-a-long-random-string-in-production}
  expiration-ms: 86400000